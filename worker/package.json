{
  "name": "myinvois-uat-proxy",
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy that logs in to LHDN MyInvois (UAT/sandbox), submits a signed e-Invoice, and polls for the validation result + UUID. Keeps the client_id/client_secret server-side and sidesteps the browser CORS wall.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/**/*.test.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen-test-cert": "node scripts/gen-test-cert.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "@types/node": "^26.5.0",
    "tsx": "^4.19.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.1"
  }
}
